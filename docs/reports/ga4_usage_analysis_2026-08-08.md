# GA4簡易利用分析（2026-08-08）

## 対象

- 集計期間: 2026-07-11〜2026-08-07（直近28日、当日分を除く）
- GA4プロパティID: `489676180`
- J1アプリ用ストリーム: `15315638495`
- 測定ID: `G-D757SHS30N`
- 確認方法: GA4標準レポートのページ・イベント集計

同じGA4プロパティに `My_Qiita_Website` と `J1試合予測AI Web・PWA・Streamlit` の2ストリームがある。プロパティ全体のホーム画面は両方を合算するため、アプリの判断には使わない。以下はJ1固有のイベントとページタイトルから確認した数値である。

## 結果

| 指標 | イベント数 | ユーザー数 | 1ユーザー当たり |
|---|---:|---:|---:|
| アプリ起動 `app_open` | 38 | 8 | 4.75 |
| 画面切替 `view_app_section` | 23 | 7 | 3.29 |
| チーム絞り込み `select_team_filter` | 7 | 4 | 1.75 |
| 試合詳細 `view_match_detail` | 4 | 3 | 1.33 |
| マイチーム設定 `set_my_team` | 3 | 3 | 1.00 |
| 節絞り込み `select_matchweek_filter` | 3 | 1 | 3.00 |
| 過去シーズン選択 `select_past_season` | 2 | 1 | 2.00 |
| インストール手順表示 `install_instructions_open` | 1 | 1 | 1.00 |

ページタイトル `Jリーグ試合予想AI｜スコア予測・勝敗予想` は、同期間で35表示、6アクティブユーザー、112イベント、直帰率27.3%だった。J1用データストリームは過去48時間以内にもデータを受信しており、計測自体は動作している。

## 読み取り

1. **利用者数はまだ小さい。** 新シーズン開幕前のデータが中心で8ユーザーのため、機能の優劣を断定する段階ではない。
2. **再訪の兆しがある。** `app_open` は1ユーザー当たり4.75回で、一度だけ開いて終わる利用だけではない。
3. **主要機能は実際に使われている。** 8ユーザー中、チーム絞り込みは4ユーザー、試合詳細とマイチーム設定は各3ユーザーが利用した。
4. **試合詳細への遷移は改善余地がある。** 起動ユーザーに対する試合詳細利用は37.5%で、試合カードが詳細を開けることをさらに明確にする価値がある。
5. **インストール評価にはデータ不足。** 手順表示は1ユーザーだけで、インストール完了イベントは上位イベントに現れていない。開幕後に再評価する。
6. **キーイベントが0件。** 現在はGA4上で成果指標が設定されていない。`view_match_detail`、`set_my_team`、`app_installed` を候補とし、母数が増えてからキーイベント化を検討する。

## 当面の判断

- 開幕後4週間は、機能追加よりも利用者数と既存機能の利用率を観察する。
- 毎週、利用者数、1人当たり起動回数、試合詳細利用率、マイチーム設定率、データ読込エラーを確認する。
- 4週間後に、マイチーム設定率と試合詳細利用率を再評価し、導線改善の要否を決める。
- Qiitaとアプリが同じプロパティにあるため、必ずストリームID `15315638495` で絞り込む。

## 注意点

- 同意した利用者だけを計測する仕様のため、実利用者全体より少なく表示される。
- 少人数では割合が大きく変動する。1〜2人の差を改善効果と断定しない。
- GA4の当日データは変動するため、定例レポートは前日までを対象にする。
